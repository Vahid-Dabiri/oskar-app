
export type ProductData = {
    imageUrl: string | undefined ,
    title: string | null,
    sku: string | null,
    price: string | null,
    seller: string | null,
    stock: string | null
}

export type ProductArray = ProductData[]
